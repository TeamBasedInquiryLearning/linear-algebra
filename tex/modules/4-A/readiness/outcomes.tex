\begin{readinessAssuranceOutcomes}
\item Solve a system of linear equations (including finding a basis of the solution space if it is homogeneous) by interpreting as an augmented matrix and row reducing \listOfStandards{E1, E2, E3, E4}.
\item State the definition of a spanning set, and determine if a set of vectors spans a vector space or subspace \listOfStandards{V3}.
\item State the definition of linear independence, and determine if a set of vectors is linearly dependent or independent \listOfStandards{S1}.
\item State the definition of a basis, and determine if a set of vectors is a basis \listOfStandards{S2}.
\end{readinessAssuranceOutcomes}

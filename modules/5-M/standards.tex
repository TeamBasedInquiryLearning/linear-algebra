\moduleQuestion{What algebraic structure do matrices have?}
\begin{standardsList}
  \standard{Matrix Multiplication}{MatMlt}{
    multiply matrices.
  }
  \standard{Invertible Matrices}{InvVer}{
    determine if a square matrix is invertible or not.
  }
  \standard{Matrix inverses}{InvCmp}{
    compute the inverse matrix of an invertible matrix.
  }
\end{standardsList}
%
%
% \moduleQuestion{How can we understand linear maps geometrically?}
% \begin{standardsList}}
%   \standard{Row operations}{Foo}{  represent a row operation as matrix multiplication, and compute how the operation affects the determinant.}
%   \standard{Determinants}{Foo}{ compute the determinant of a square matrix.}
%   \standard{Eigenvalues}{Foo}{ find the eigenvalues of a $2\times 2$ matrix.}
%   \standard{Eigenvectors}{Foo}{ find a basis for the eigenspace of a square matrix associated with a given eigenvalue.
% \end{standardsList}

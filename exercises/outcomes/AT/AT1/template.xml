<?xml version='1.0' encoding='UTF-8'?>
<knowl mode="exercise" xmlns="https://spatext.clontz.org" version="0.2">
    <content>
        <p>
        Consider the following maps of polynomials <m>S:\mathcal{P}\rightarrow\mathcal{P}</m>
        and <m>T:\mathcal{P}\rightarrow\mathcal{P}</m> defined by
        <!-- {{#swapped}} -->
        <me>
            S({{f_letter}}(x))=
                    {{nonlinear_trans}}
                \hspace{1em} \text{and} \hspace{1em}
            T({{f_letter}}(x))=
                    {{linear_trans}}
        </me>
        <!-- {{/swapped}} -->
        <!-- {{^swapped}} -->
        <me>
            S({{f_letter}}(x))=
                    {{linear_trans}}
                \hspace{1em} \text{and} \hspace{1em}
            T({{f_letter}}(x))=
                    {{nonlinear_trans}}
        </me>
        <!-- {{/swapped}} -->
        Explain why one these maps is a linear transformation and why the other map is not.</p>
    </content>
    <outtro>
      <!-- {{#swapped}} -->
            <p><m>S</m> is not linear and <m>T</m> is linear.</p>
      <!-- {{/swapped}} -->
      <!-- {{^swapped}} -->
            <p><m>S</m> is linear and <m>T</m> is not linear.</p>
      <!-- {{/swapped}} -->
    </outtro>
</knowl>
